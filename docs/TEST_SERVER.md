# 测试环境凭证（内部开发者使用）

> 下面是用于本机验收/联调的真实测试环境。**这是测试服务器，可以随意操作**（安装/删除/启动/停止、创建临时站点、改防火墙、操作数据库等均可）。
>
> ⚠️ 安全提示：这些凭据仅存放在本文档，供团队内部开发者直接查看。**不得**写入源码、`fixtures/`、日志、诊断包或命令参数。生产环境请使用企业自己的凭据管理。

## SSH 测试服务器

| 字段 | 值 |
|---|---|
| 主机 | `8.138.151.118` |
| SSH 端口 | `22` |
| 用户名 | `root` |
| 密码 | `@Wingeng1218` |
| 说明 | 测试服务器，可随意操作 |

## 1Panel 管理系统（Web 面板）

| 字段 | 值 |
|---|---|
| 地址 | http://8.138.151.118:28085/8fefacfdc6 |
| 用户名 | `root` |
| 密码 | `@Wingeng1218` |
| 说明 | 已登录；用于对标 1Panel 社区版 UI 与操作流程。注意：面板启用了安全入口（`/8fefacfdc6`），**必须用 HTTP**（HTTPS 在 28085 端口不可用）；入口变化时在测试服务器上运行 `1pctl user-info` 获取最新地址。`https://panel.wingeng.xyz/` 仅显示安全入口提示页。 |

## 使用约定

- 每次真实操作（新建/删除/改防火墙/装包/动数据库）先在测试环境执行，并记录临时资源的创建、验证与清理结果。
- 涉及真实远端写操作前，用户需在客户端 UI 显式确认；`confirmed` 一律由用户触发，客户端不在未经确认的情况下执行破坏性远端命令。
- 首次连接必须人工核对 Host Key 指纹。
